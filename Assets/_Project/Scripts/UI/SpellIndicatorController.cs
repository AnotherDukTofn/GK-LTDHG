using UnityEngine;
using System;

namespace SpellStrike.UI
{
    /// <summary>
    /// NOTE: Toàn bộ logic hiển thị Indicator đã được chuyển (refactor) thẳng vào PlayerSpellController.
    /// Script này không còn tác dụng và được giữ lại tạm thời dưới dạng rỗng để tránh lỗi Missing Script trên các Prefab cũ.
    /// Bạn có thể xóa file này an toàn khỏi project.
    /// </summary>
    [Obsolete("Logic đã được chuyển thẳng vào PlayerSpellController. Bạn có thể xóa Script này và GameObject SpellIndicatorSystem cũ.")]
    public class SpellIndicatorController : MonoBehaviour
    {
        // Rỗng
    }
}
